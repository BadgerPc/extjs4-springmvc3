CREATE TABLE Chapter (id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), number INTEGER NOT NULL, title VARCHAR(100) NOT NULL, bookId INTEGER NOT NULL, PRIMARY KEY (id), FOREIGN KEY (bookId) REFERENCES Book(id));